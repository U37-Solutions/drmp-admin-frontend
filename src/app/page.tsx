import { Typography } from 'antd';

import { MainLayout } from '@/app/components';

export default function Home() {
  return (
    <MainLayout>
      <section>
        <Typography>DRMP Admin Panel</Typography>
      </section>
    </MainLayout>
  );
}
